import { describe, it, expect } from 'vitest';

// import { vi, afterEach } from 'vitest';
// import { APIClient } from '../apiClient';
// import { AuthStore } from '../../stores/authStore';

// vi.mock('axios', () => ({
//   default: {
//     create: vi.fn(() => ({
//       interceptors: {
//         request: { use: vi.fn() },
//         response: { use: vi.fn() },
//       },
//       get: vi.fn(),
//       post: vi.fn(),
//       put: vi.fn(),
//       delete: vi.fn(),
//     })),
//   },
// }));

describe('Temporarily disabled', () => {
  it('should be enabled later', () => {
    expect(true).toBe(true);
  });
});